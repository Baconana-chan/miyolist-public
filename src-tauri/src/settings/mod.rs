pub fn foundation_summary() -> crate::models::FoundationModule {
    crate::models::FoundationModule {
        name: "Settings".into(),
        summary: "User-visible runtime rules explain what is local, what is synced, and what never leaves the device.".into(),
        status: "ready".into(),
    }
}
